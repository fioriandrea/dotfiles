general {
    output_format = "i3bar"
    colors = true
    interval = 20
}

order += "battery all"
order += "tztime local"

battery all {
    last_full_capacity = true
    format = "%status %percentage"
    format_down = ""
}

tztime local {
    format = "%Y-%m-%d %H:%M"
}
