[ids]
# Match only the Lenovo's main keyboard event node. Do not grab its mouse or
# Consumer Control/TrackPoint interfaces; hwdb and libinput handle those.
k:17ef:6080:46ad7983

[main]
# Tap Caps Lock normally; hold it as Super/Meta for shortcuts such as Super+X.
capslock = overload(meta, capslock)
